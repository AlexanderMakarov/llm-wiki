# AWOS note — #182 shipped in branch `feat/182-configurable-adapters`

- `llmwiki/adapters/settings.py` — unified `adapters.<name>` config + `select_sync_adapters()`
- `llmwiki configure-sources` — interactive enablement; `setup.sh` optional TTY prompt
- Bare sync / adapters / watch share selection logic; `enabled: false` honoured
