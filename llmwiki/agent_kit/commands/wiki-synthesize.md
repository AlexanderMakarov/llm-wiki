Deprecated alias for `/wiki-synth`. Prefer that command.

This wrapper still runs:

```
python3 -m llmwiki synthesize
```

(sources-only + deprecation warning) for old habits. New work should use `python3 -m llmwiki synth`.
